# Secrets
.env
.env.*
!.env.example

# Dataset — large, and not ours to redistribute
data/raw/
data/derived/

# Agent outputs (regenerated by src/eval/run_cases.py)
# NOT cases/ -- the README requires the 20 graded answer files to live in a
# folder literally called cases/ at the repo root; that folder IS committed.
out/

# Python
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Tooling
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
